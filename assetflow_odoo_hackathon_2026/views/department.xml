<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- List View -->
    <record id="view_department_list" model="ir.ui.view">
        <field name="name">assetflow.department.list</field>
        <field name="model">assetflow.department</field>
        <field name="arch" type="xml">
            <list string="Departments">
                <field name="code"/>
                <field name="name"/>
                <field name="manager_id"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_department_form" model="ir.ui.view">
        <field name="name">assetflow.department.form</field>
        <field name="model">assetflow.department</field>
        <field name="arch" type="xml">
            <form string="Department">
                <sheet>
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only"/>
                        <h1>
                            <field name="name" placeholder="e.g. IT Department"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="code"/>
                            <field name="manager_id" options="{'no_create': True}"/>
                        </group>
                        <group>
                            <field name="active" widget="boolean_toggle"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Employees" name="employees">
                            <field name="employee_ids" readonly="1">
                                <list>
<!--                                    <field name="employee_code"/>-->
                                    <field name="name"/>
                                    <field name="work_email"/>
                                    <field name="work_phone"/>
<!--                                    <field name="asset_role"/>-->
<!--                                    <field name="status"/>-->
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
<record id="view_department_search" model="ir.ui.view">
    <field name="name">assetflow.department.search</field>
    <field name="model">assetflow.department</field>
    <field name="arch" type="xml">
        <search>
            <field name="name"/>
            <filter name="active"
                    string="Active"
                    domain="[('active','=',True)]"/>
            <filter name="inactive"
                    string="Inactive"
                    domain="[('active','=',False)]"/>
        </search>
    </field>
</record>

    <!-- Action -->
    <record id="action_department" model="ir.actions.act_window">
        <field name="name">Departments</field>
        <field name="res_model">assetflow.department</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_department_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new Department.
            </p>
            <p>
                Departments help group employees and manage asset allocations.
            </p>
        </field>
    </record>

    <!-- Root Menu -->
    <menuitem id="menu_assetflow_root"
              name="AssetFlow"
              web_icon="assetflow_odoo_hackathon_2026,static/description/icon.png"
              sequence="10"/>

    <!-- Configuration Submenu -->
    <menuitem id="menu_assetflow_config"
              name="Configuration"
              parent="menu_assetflow_root"
              sequence="100"/>

    <!-- Department Menu -->
    <menuitem id="menu_assetflow_department"
              name="Departments"
              parent="menu_assetflow_config"
              action="action_department"
              sequence="10"/>

</odoo>